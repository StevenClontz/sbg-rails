<%= form_for [standard_category.course,standard_category] do |f| %>
  <% if standard_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(standard_category.errors.count, "error") %> prohibited this standard_category from being saved:</h2>

      <ul>
      <% standard_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :satsifactory_limit %>
    <%= f.number_field :satsifactory_limit %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
